/**
 * AUTOR:GABRIEL GAMELEIRA DOS SANTOS 
 * DATA: 10/05/2020
 * HORA: 17:58
 * FINALIDADE DO PROGRAMA: AVALIA O CONCEITO DA NOTA EM UM VETOR DE NOTAS
 */

const trocarPosicoesVetores = (vetor_um = [], vetor_dois = []) => {

    if (vetor_um.length == vetor_dois.length) {

        vetor_um.forEach((valor) => {

            vetor_dois.push(valor);

            vetor_um

        })
    }
}